# 开发服务器监听。0.0.0.0 / true = 局域网可访问；127.0.0.1 / false = 仅本机
VITE_DEV_HOST=0.0.0.0
VITE_DEV_PORT=5173

# 前端 /api 代理到后端（本机回环即可，不要改成局域网 IP）
VITE_API_PROXY=http://127.0.0.1:9808

# Vite Host 校验。true = 允许任意 Host；或写逗号分隔的主机名
VITE_ALLOWED_HOSTS=true
